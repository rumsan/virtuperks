export type TaskDetail = {
  allowedWallets: string[];
  detailsUrl: string;
  expiryDate: string;
  id: string;
  isActive: boolean;
  maxParticipants: string;
  owner: string;
  rewardAmount: string;
  rewardToken: string;
};
