export type TaskDetail = {
  allowedWallets: string[];
  taskName: string;
  detailsUrl: string;
  expiryDate: string;
  id: string;
  isActive: boolean;
  maxParticipants: string;
  owner: string;
  rewardAmount: string;
  rewardToken: string;
};
